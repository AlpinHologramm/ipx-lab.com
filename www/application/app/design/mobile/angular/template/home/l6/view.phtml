<div class="tabbar_items l6" ng-class="{'toggle': animate_tabbar}" ng-show="tabbar_is_visible">

    <div id="tabbar_background" class="tabbar background"></div>

    <div class="tabbar no-background">
        <ul class="tabbar_l6">
            <li ng-repeat="option in features.overview.options" class="border-color">
                <a href="{{ option.url }}" class="item" ng-class="{ is_link: option.is_link }">
                    <img ng-src="{{ option.icon_url }}" ng-class="{ colorable: option.icon_is_colorable }" width="30" height="30" />
                    <span class="block">{{ option.name }}</span>
                </a>
            </li>
        </ul>
    </div>
</div>